{
  "name": "expense-tracker-pennywise",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend --include=dev && npm install --prefix client --include=dev && npm run build --prefix client && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  }
}
